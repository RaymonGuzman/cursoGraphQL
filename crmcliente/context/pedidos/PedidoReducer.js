import { 
  SELECCIONAR_CLIENTE,
  SELECCIONAR_PRODUCTO,
  CANTIDAD_PRODUCTOS
 } from "../../types";

 export default ( state, action ) => {
   switch (action.type) {
     case value:
       
       break;
   
     default:
       return state;
   }
 }